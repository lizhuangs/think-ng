import { TkTableDirective } from './tk-table.directive';

describe('TkTableDirective', () => {
  it('should create an instance', () => {
    const directive = new TkTableDirective();
    expect(directive).toBeTruthy();
  });
});
